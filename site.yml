# file: site.yml
---
- hosts: all
  become: yes
  roles:
    - geerlingguy.repo-epel
    - geerlingguy.pip
    # - geerlingguy.certbot
    # - geerlingguy.nginx
    - roles/crowley.cloud
  tasks:
    - name: Flush handlers in case any configs have changed.
      meta: flush_handlers