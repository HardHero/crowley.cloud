# file: site.yml
---
- hosts: all
  become: yes
  roles:
    - geerlingguy.repo-epel
    - geerlingguy.pip
    - roles/crowley.cloud
  tasks:
    - name: Flush handlers in case any configs have changed.
      meta: flush_handlers
